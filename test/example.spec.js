import { expect } from 'chai';
// import sinon from 'sinon';

describe('Example test', () => {
    it('should run successfully', () => {
        expect(2).to.equal(2);
    });

    it('should run successfully too', () => {
        expect('ahoj').to.equal('ahoj');
    });
});
